/**
 * @description: Type for the user
 */
export interface UserLoginFormType {
    username: string;
    password: string;
}

/**
 * @description: Type for the user
 */
export interface UserAuthType {
    user_id: string;
    username: string;
    email: string;
}
